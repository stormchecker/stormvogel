name: Publish nightly Docker image

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  push_to_registry:
    name: ${{ github.event_name == 'push' && 'Push' || 'Build' }} nightly Docker image
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
      attestations: write
      id-token: write
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@f4ef78c080cd8ba55a85445d5b36e214a81df20a
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        id: push
        uses: docker/build-push-action@3b5e8027fcad23fda98b2e3ac259d8d67585f671
        with:
          context: .
          file: ./Dockerfile
          push: ${{ github.event_name == 'push' }}
          pull: true
          tags: stormvogel/stormvogel:nightly

      - name: Generate artifact attestation
        if: github.event_name == 'push'
        uses: actions/attest-build-provenance@v2
        with:
          subject-name: index.docker.io/stormvogel/stormvogel
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true

  push_experimental:
    name: ${{ github.event_name == 'push' && 'Push' || 'Build' }} experimental Docker image
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
      attestations: write
      id-token: write
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Download stormpy nightly wheel
        uses: dawidd6/action-download-artifact@v6
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          workflow: create_wheel_scheduled.yml
          repo: stormchecker/stormpy
          name: stormpy-wheels-linux-intel
          path: stormpy_wheel/

      - name: Log in to Docker Hub
        uses: docker/login-action@f4ef78c080cd8ba55a85445d5b36e214a81df20a
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        id: push
        uses: docker/build-push-action@3b5e8027fcad23fda98b2e3ac259d8d67585f671
        with:
          context: .
          file: ./Dockerfile
          push: ${{ github.event_name == 'push' }}
          pull: true
          tags: stormvogel/stormvogel:stormvogel-experimental
          build-args: STORMPY_WHEEL=1

      - name: Generate artifact attestation
        if: github.event_name == 'push'
        uses: actions/attest-build-provenance@v2
        with:
          subject-name: index.docker.io/stormvogel/stormvogel
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true
